import { citySelect } from './exporting.mjs'

citySelect.selectCity("lucknow")

// here if we want the cities variable in exporting to have new values, we have to change the variable by going inside the file, which is wrong